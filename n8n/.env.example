# LLM providers for n8n workflows are configured via n8n credentials, not via
# environment variables.
# Coach imports are templates: after importing `n8n/workflows/coach.json`,
# manually rebind the 6 DeepSeek nodes to a real DeepSeek credential before
# enabling the workflow.
# Supabase must also target POST /webhook/coach and allow the n8n host through
# WEBHOOK_ALLOWED_HOSTS on the Edge Function side.
SUPABASE_URL=https://qpogulljnnacrxdjbwiz.supabase.co
PHASE2_WEBHOOK_HMAC_SECRET=
